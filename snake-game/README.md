# Snake Game

A simple Snake game built with Python and Pygame.

## Run it

1. Install the dependency:

   ```bash
   python -m pip install -r requirements.txt
   ```

2. Start the game:

   ```bash
   python main.py
   ```

Use the arrow keys or **WASD** to move. After losing, press **R** to restart or **Q** to quit.
